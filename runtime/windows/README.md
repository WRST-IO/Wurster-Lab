# Windows Runtime Output

`npm run dist:win` writes Windows Wurster installers here under `runtime/windows/dist/`.
The shared Electron implementation lives in `runtime/desktop/`; Windows-specific release signing is configured through `.env.signing.local`.
